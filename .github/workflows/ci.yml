name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DENO_NO_UPDATE_CHECK: "1"

jobs:
  test:
    name: Test / ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            os: ubuntu-24.04
          - name: Windows x86_64
            os: windows-2022
          - name: macOS arm64
            os: macos-14
          - name: macOS x86_64
            os: macos-15-intel

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.9.5

      - name: Show tool versions
        run: |
          deno --version
          ssh -V

      - name: Run source checks and unit tests
        run: deno task check

  toolchain-mise:
    uses: ./.github/workflows/toolchain-mise.yml

  toolchain-nvm:
    uses: ./.github/workflows/toolchain-nvm.yml

  package:
    name: Package / ${{ matrix.name }}
    if: github.event_name != 'pull_request'
    needs:
      - test
      - toolchain-mise
      - toolchain-nvm
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows CEF x86_64 (cross-build)
            os: ubuntu-24.04
            package_task: package:windows:cef
            artifact_name: dsh-desktop-windows-cef-x86_64
            artifact_path: dist/windows/DSH-Desktop-windows-x86_64-cef.zip
          - name: Windows WebView2 x86_64 (cross-build)
            os: ubuntu-24.04
            package_task: package:windows:webview
            artifact_name: dsh-desktop-windows-webview-x86_64
            artifact_path: dist/windows/DSH-Desktop-windows-x86_64-webview.zip
          - name: macOS arm64 (cross-build)
            os: ubuntu-24.04
            package_task: package:macos:aarch64
            artifact_name: dsh-desktop-macos-arm64
            artifact_path: dist/macos/DSH-Desktop-macos-aarch64.tar.gz
          - name: macOS x86_64 (cross-build)
            os: ubuntu-24.04
            package_task: package:macos:x86_64
            artifact_name: dsh-desktop-macos-x86_64
            artifact_path: dist/macos/DSH-Desktop-macos-x86_64.tar.gz

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.9.5

      - name: Build native desktop distribution
        run: deno task ${{ matrix.package_task }}

      - name: Upload desktop distribution
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: error
          retention-days: 7
          compression-level: 0

  release:
    name: Update latest release
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    needs: package
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Download all release assets
        uses: actions/download-artifact@v8
        with:
          pattern: dsh-desktop-*
          path: release-assets
          merge-multiple: true

      - name: Verify release assets
        shell: bash
        run: |
          set -euo pipefail
          test -s release-assets/DSH-Desktop-windows-x86_64-cef.zip
          test -s release-assets/DSH-Desktop-windows-x86_64-webview.zip
          test -s release-assets/DSH-Desktop-macos-aarch64.tar.gz
          test -s release-assets/DSH-Desktop-macos-x86_64.tar.gz
          ls -lh release-assets

      - name: Create or replace latest release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          current_main=$(gh api "repos/${GITHUB_REPOSITORY}/git/ref/heads/main" --jq '.object.sha')
          if [[ "${current_main}" != "${GITHUB_SHA}" ]]; then
            echo "::notice::Skipping stale release for ${GITHUB_SHA}; main is ${current_main}."
            exit 0
          fi

          assets=(
            release-assets/DSH-Desktop-windows-x86_64-cef.zip
            release-assets/DSH-Desktop-windows-x86_64-webview.zip
            release-assets/DSH-Desktop-macos-aarch64.tar.gz
            release-assets/DSH-Desktop-macos-x86_64.tar.gz
          )
          cat > release-notes.md <<EOF
          This rolling release was built from commit \`${GITHUB_SHA}\`.

          It is updated only after all Linux, Windows, and macOS test jobs plus all
          Windows CEF, Windows WebView2, and macOS distribution builds complete successfully.

          Workflow run: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}
          EOF

          if gh release view latest >/dev/null 2>&1; then
            gh release upload latest "${assets[@]}" --clobber
            git tag --force latest "${GITHUB_SHA}"
            git push origin refs/tags/latest --force
            gh release edit latest \
              --target "${GITHUB_SHA}" \
              --title "DSH Desktop - Latest Successful Build" \
              --notes-file release-notes.md \
              --latest
          else
            gh release create latest "${assets[@]}" \
              --target "${GITHUB_SHA}" \
              --title "DSH Desktop - Latest Successful Build" \
              --notes-file release-notes.md \
              --latest
          fi
